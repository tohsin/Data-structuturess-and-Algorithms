def longestPalindrome(self, s: str) -> int:
    for i in range(len())
def isPalidrome(guy):
    return guy==guy[::-1]
def expandodd(s,left,right):
        dist1=0
        while left>0 and right <len(s):
            if  isPalidrome(s[left:right]):
                dist1 =right-left
