#include <iostream>
#include <vector>
using namespace std;

void solver(){

}
void recursivesolver(vector<int> curr_soln,int index,int d){
    // for(int i=index;i<curr_soln.size();++i){
        int diff=curr_soln[index-1]+d
        for(int j=curr_soln[index-1]+1;j<diff;++j){
            curr_soln.push_back(j);
        }
        if(curr_soln[i])
     
    // }

}

int main(){

}